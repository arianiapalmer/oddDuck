var Product = function (name, path, timeShown) {
    this.name = name;
    this.path = path;
    this.timeShown = timeShown;
}
Product.prototype.generateRandomImg(Product){//how to generate random new product to keep track of times shown. do i nest them in an array?
    (for i = 0; i < img.length; i++);
    let timeShown = 0;



}
var bag = new Product(bag, img/bag.jpg, );


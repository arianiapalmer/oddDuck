var product = function (name, path, timeShown) {
    this.name = name;
    this.path = path;
    this.timeShown = timeShown;
}

var bag = new product(bag, img/bag.jpg, )

product.prototype.generateRandom(img){

}